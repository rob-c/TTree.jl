# `TTree`

The top-level module: opening and creating files, and the names re-exported
from the submodules below.

```@docs
TTree
```

```@autodocs
Modules = [TTree]
Order = [:constant, :type, :function]
```
